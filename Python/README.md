# Randomer Forest Python Package

## Requirements

1. C++ compiler (gcc)

  ```sh
  sudo apt-get install build-essential      # Ubuntu/Debian
  ```

## Installation

`pip install -e git+https://github.io/neurodata/rerf/#egg=rerf&subdirectory=Python`